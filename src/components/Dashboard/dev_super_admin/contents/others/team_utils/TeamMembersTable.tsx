"use client";

type TQueryObj = {
  designationId?: string;
  departmentId?: string;
  status?: string;
  isPublished?: string;
  searchTerm?: string;
  page?: number;
  limit?: number;
};
const TeamMembersTable = () => {
  return <div></div>;
};

export default TeamMembersTable;
